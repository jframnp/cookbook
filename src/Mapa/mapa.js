import {Text, View, ScrollView} from 'react-native';

export default function Mapa() {
  return (
      <ScrollView>
        <View>
          <Text>Mapa</Text>
        </View>
      </ScrollView>

    
  );
}